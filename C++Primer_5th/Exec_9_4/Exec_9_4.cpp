bool find(vector<int>::const_iterator begin, vector<int>::const_iterator end, int i)
{
    for ()
    while ( begin++ != end)
    {
        if (*begin == i)
            return ture;
    }
    return false;
}
